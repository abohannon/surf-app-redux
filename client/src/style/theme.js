import { createMuiTheme } from '@material-ui/core/styles'

const mainTheme = createMuiTheme()
const secondaryTheme = createMuiTheme()

mainTheme.palette.common.black = `#2a2a2a`

export {
  mainTheme,
  secondaryTheme,
}
